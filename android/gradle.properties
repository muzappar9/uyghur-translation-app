org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8
android.useAndroidX=true
android.enableJetifier=true

# 允许中文路径 (解决non-ASCII characters警告)
android.overridePathCheck=true

# ============================================
# 🔑 关键TLS修复配置 - 解决握手失败
# ============================================
# 强制启用TLSv1.2和TLSv1.3支持
systemProp.https.protocols=TLSv1.2,TLSv1.3

# 启用所有安全算法套件（处理客户端和服务器不兼容）
systemProp.jdk.tls.client.protocols=TLSv1.2,TLSv1.3

# ============================================
# 网络和超时优化
# ============================================
# 延长Gradle操作超时时间
org.gradle.internal.http.connectionTimeout=120000
org.gradle.internal.http.socketTimeout=120000

# 国内镜像源配置
systemProp.http.proxyHost=
systemProp.http.proxyPort=
systemProp.https.proxyHost=
systemProp.https.proxyPort=

# gradle 下载镜像源 - 使用官方（8.4已缓存）
# org.gradle.internal.repository.initial.default.repo.url=https\://services.gradle.org/distributions/

# ============================================
# 代码风格和编译器配置
# ============================================
kotlin.code.style=official

# ============================================
# 禁用不需要的验证（加速构建）
# ============================================
android.nonTransitiveRClass=false

# ============================================
# 🔧 gradle-api JAR生成跳过配置
# ============================================
# 禁用gradle-api JAR的自动生成（Gradle 8.4已知问题）
# 这些选项强制gradle daemon使用预缓存版本
org.gradle.build.parallelism=4
org.gradle.parallel=true
org.gradle.parallel.workers=4
org.gradle.configureondemand=true
org.gradle.caching=true
org.gradle.caching.debug=false

# 禁用gradle内部任务缓存生成
org.gradle.internal.launcher.welcomeMessageEnabled=false
org.gradle.internal.scan.enabled=false
org.gradle.internal.fork=true

