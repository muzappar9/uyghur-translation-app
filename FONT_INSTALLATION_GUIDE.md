# Alkatip 字体安装指南

## 📝 概述

本项目支持 **10 种 Alkatip 维吾尔语字体** 和 **5 种常见汉语字体**，用户可以在翻译结果中随意切换。

---

## 📂 字体文件结构

```
assets/fonts/
├── alkatip/           # 维吾尔语 Alkatip 字体
│   ├── Alkatip.ttf                    # 1. 标准字体
│   ├── AlkatipKona.ttf                # 2. 经典版
│   ├── AlkatipTor.ttf                 # 3. 粗体
│   ├── AlkatipYumilaq.ttf             # 4. 圆润体
│   ├── AlkatipNazik.ttf               # 5. 细体
│   ├── AlkatipBasma.ttf               # 6. 印刷体
│   ├── AlkatipTarixi.ttf              # 7. 古典体
│   ├── AlkatipQol.ttf                 # 8. 手写体
│   ├── AlkatipKompyuter.ttf           # 9. 计算机体
│   └── AlkatipChong.ttf               # 10. 大字体
│
└── chinese/           # 汉语字体
    ├── SourceHanSansSC-Regular.otf    # 思源黑体 常规
    ├── SourceHanSansSC-Bold.otf       # 思源黑体 粗体
    ├── SourceHanSerifSC-Regular.otf   # 思源宋体 常规
    ├── SourceHanSerifSC-Bold.otf      # 思源宋体 粗体
    ├── ZhanKuKuaiLe-Regular.ttf       # 站酷快乐体
    ├── FZKai.ttf                      # 方正楷体
    └── FZHei.ttf                      # 方正黑体
```

---

## 🔽 字体下载来源

### Alkatip 字体

Alkatip 是维吾尔语最流行的字体系列，您需要：

1. **官方来源**（推荐）:
   - 访问 Alkatip 官方网站
   - 或从维吾尔语社区获取正版字体

2. **开源替代** (如果无法获取原版):
   - Noto Sans Arabic (Google)
   - Scheherazade New (SIL)
   
3. **重要**: 将字体文件重命名为上述文件名

### 汉语字体

1. **思源黑体/宋体** (免费开源):
   ```bash
   # 下载地址
   https://github.com/adobe-fonts/source-han-sans
   https://github.com/adobe-fonts/source-han-serif
   ```

2. **站酷快乐体** (免费):
   ```bash
   https://www.zcool.com.cn/special/zcoolfonts/
   ```

3. **方正字体** (商业授权):
   - 需要购买授权或使用免费版本

---

## 📥 安装步骤

### 🚀 方法 1: 自动下载安装 (推荐)

**一键自动下载所有免费开源字体**:

```powershell
# 下载所有字体（维吾尔语 + 汉语）
.\download_fonts.ps1

# 仅下载维吾尔语字体
.\download_fonts.ps1 -SkipChinese

# 强制重新下载
.\download_fonts.ps1 -Force
```

**自动下载的字体包括**:
- ✅ Noto Sans Arabic (作为 Alkatip 的开源替代)
- ✅ 思源黑体 (Regular + Bold)
- ✅ 思源宋体 (Regular + Bold)
- ✅ 站酷快乐体

### 📋 方法 2: 手动安装

#### 步骤 1: 创建字体文件夹

```powershell
mkdir assets/fonts/alkatip
mkdir assets/fonts/chinese
```

#### 步骤 2: 下载字体文件

**维吾尔语字体 (Alkatip)**:
- 访问 Alkatip 官方网站购买正版
- 或使用 Noto Sans Arabic 免费替代

**汉语字体**:
- 思源字体: https://github.com/adobe-fonts
- 站酷字体: https://www.zcool.com.cn/special/zcoolfonts/

#### 步骤 3: 放置字体文件

将下载的字体文件放置到对应文件夹。

#### 步骤 4: 验证字体文件

```powershell
# 运行检查脚本
.\install_fonts.ps1
```

#### 步骤 5: 重新构建应用

```powershell
flutter clean
flutter pub get
flutter run
```

---

## ⚠️ 重要说明

### 如果缺少字体文件

应用仍然可以运行，但会：
- 使用系统默认字体显示文本
- 字体选择器中对应的字体选项不会生效

### 临时测试方案

如果暂时没有字体文件，可以：

1. **使用占位字体**: 复制系统字体并重命名
2. **注释配置**: 在 `pubspec.yaml` 中注释掉缺失的字体
3. **仅使用系统字体**: 在设置中选择"系统默认"

---

## 🎨 使用方法

### 在代码中使用

```dart
import 'package:uyghur_translator/widgets/translation_text_card.dart';

// 显示翻译结果（自动应用用户选择的字体）
TranslationTextCard(
  text: 'مەرھابا دۇنيا',
  languageCode: 'ug',
  label: '维吾尔语',
  showFontButton: true,  // 显示字体切换按钮
)

// 双语对照显示
BilingualTextCard(
  sourceText: '你好世界',
  sourceLang: 'zh',
  targetText: 'مەرھابا دۇنيا',
  targetLang: 'ug',
  showFontButtons: true,
)
```

### 打开字体设置

```dart
// 方法 1: 使用底部表单
FontSettingsSheet.show(context);

// 方法 2: 使用快速切换按钮
QuickFontSwitcher(languageCode: 'ug')
```

---

## 🛠️ 字体配置 API

### 通过 Provider 访问

```dart
// 读取当前字体配置
final fontConfig = ref.watch(currentFontConfigProvider);

// 切换维吾尔语字体
ref.read(fontConfigProvider.notifier).setUyghurFont(AlkatipFont.kona);

// 切换汉语字体
ref.read(fontConfigProvider.notifier).setChineseFont(ChineseFont.sourceHanSans);

// 调整字体大小
ref.read(fontConfigProvider.notifier).setFontSize(18.0);

// 重置为默认
ref.read(fontConfigProvider.notifier).resetToDefault();
```

---

## 📋 字体列表

### Alkatip 字体

| 序号 | 字体名称 | 中文名 | 维吾尔语名 | 推荐用途 |
|------|---------|--------|-----------|---------|
| 1 | Alkatip | 标准 | ئالكاتىپ ئاساسى | 通用 |
| 2 | AlkatipKona | 经典 | ئالكاتىپ كونا | 正式文档 |
| 3 | AlkatipTor | 粗体 | ئالكاتىپ توم | 标题强调 |
| 4 | AlkatipYumilaq | 圆润 | ئالكاتىپ يۇملاق | 轻松阅读 |
| 5 | AlkatipNazik | 细体 | ئالكاتىپ نازىك | 优雅显示 |
| 6 | AlkatipBasma | 印刷 | ئالكاتىپ بەسمە | 印刷品 |
| 7 | AlkatipTarixi | 古典 | ئالكاتىپ تارىخى | 古典文献 |
| 8 | AlkatipQol | 手写 | ئالكاتىپ قول | 个人笔记 |
| 9 | AlkatipKompyuter | 计算机 | ئالكاتىپ كومپيۇتېر | 代码/技术 |
| 10 | AlkatipChong | 大字 | ئالكاتىپ چوڭ | 大标题 |

### 汉语字体

| 序号 | 字体名称 | 说明 | 推荐用途 |
|------|---------|------|---------|
| 1 | System | 系统默认 | 通用 |
| 2 | SourceHanSansSC | 思源黑体 | 现代文本 |
| 3 | SourceHanSerifSC | 思源宋体 | 正式文档 |
| 4 | ZhanKuKuaiLe | 站酷快乐体 | 活泼风格 |
| 5 | FZKai | 方正楷体 | 传统文档 |
| 6 | FZHei | 方正黑体 | 商务文档 |
| 7 | MicrosoftYaHei | 微软雅黑 | 系统自带 |
| 8 | SimSun | 宋体 | 系统自带 |

---

## 🔍 故障排查

### 问题: 字体不显示

**解决方案**:
1. 检查文件路径和文件名
2. 确保文件格式正确 (.ttf 或 .otf)
3. 运行 `flutter clean && flutter pub get`
4. 检查 `pubspec.yaml` 缩进是否正确

### 问题: 某些字符显示为方块

**解决方案**:
1. 确认字体文件支持维吾尔语字符集
2. 使用 Noto Sans Arabic 作为备用字体
3. 检查文本编码是否为 UTF-8

### 问题: 字体切换不生效

**解决方案**:
1. 热重启应用 (不是热重载)
2. 清除应用数据
3. 检查 SharedPreferences 是否正常工作

---

## 📝 许可证

- **Alkatip 字体**: 请遵循原字体的许可协议
- **思源字体**: SIL Open Font License 1.1
- **其他字体**: 各自的许可协议

**重要**: 商业使用前请确认字体授权！

---

## 🚀 下一步

1. ✅ 字体系统已完全集成
2. ✅ UI 组件已创建
3. ⏳ 需要下载并放置实际字体文件
4. ⏳ 测试所有字体的显示效果

---

**提示**: 如果您有 Alkatip 字体的合法副本，请将其放入 `assets/fonts/alkatip/` 文件夹。如果暂时没有，应用会使用系统默认字体。
